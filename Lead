<!DOCTYPE html>
<html lang="en">
 <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <h1>JavaScript Basics Coding Challange</h1>
 </head>
 <body>
    <h1>JavaScript Basics Coding Challange</h1>
    <script src="cc_1.js"></script>
 </body>   
</html>

// Declare variables
let employeeName = "Nya"; // string
const employeeID = "U19669216"; // string
var isActive = true; // boolean

// Log each variable and its type to the console
console.log ("employeeName:", employeeName, "- Type", typeof employeeName);
console.log ("employeeID:", employeeID, "- Type", typeof employeeID);
console.log ("isActive:", isActive, "- Type", typeof isActive);

// Declare variables
let productName = "Laptop"; // string
const productPrice = 999.99; // number
var isAvailable = true; // boolean

// Log each variable and its type to the console
console.log ("productName:", productName, "- Type", typeof productName);
console.log ("productPrice:", productPrice, "- Type", typeof productPrice);
console.log ("isAvailable:", isAvailable, "- Type", typeof isAvailable);

// Declare a variable for the initial balance
let accountBalance = 1000; // Intial balance

//log initial balance
console.log("Initial accountBalance:", accountBalance);

// Perform arithmetic operations and log the results
accountBalance += 200; // Add 200
console.log("After addition of 200, accountBalance:", accountBalance);

accountBalance -= 50; // Subtract 50
console.log("After subtraction of 50, accountBalance:", accountBalance);

accountBalance *= 2; // Multiply by 2
console.log("After multiplication by 2, accountBalance:", accountBalance);

accountBalance /= 4; // Divide by 4
console.log("After division by 4, accountBalance:", accountBalance);

// Declare a variable for the customer's name
let customerName = "Alice"; // string

// Concatenate the customer's name with a welcome message
let welcomeMessage = "Welcome, " + customerName + "!"; // string

// Log the welcome message to the console
console.log(welcomeMessage);

// Declare a variable for user login status
let isLoggedIn = true; // boolean

// Use logical operators to check conditions
let hasAdminAccess = false; // boolean

// Check if the user is logged in and has admin access
let canAccessAdminPanel = isLoggedIn && hasAdminAccess; // boolean
console.log("Can access admin panel:", canAccessAdminPanel);

// Check if the user is logged in or has admin access
let canAccessAdminPanel = isLoggedIn || hasAdminAccess; // boolean

// Negate the user's login status
let isLoggedOut = !isLoggedIn; // boolean
console.log("Is logged out:", isLoggedOut);

git status
git add .
git commit -m "JavaScript Basics Coding Challange"
git push -u origin main
